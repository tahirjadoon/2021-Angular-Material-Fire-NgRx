https://material.angular.io/

Prebuilt and styles components. Here is complete list with usage examples 
https://material.angular.io/components/categories 

Each functionality is built with its own project. 

Each project has an associated text document with the same name that details the steps taken for that project

If you download any project then must run >npm install first


FireBase:
	For setup see: "003 Setting Up FireBase.txt"
	
	For setting up the FireBase info at the project level see: "prj012FitnessTracker-firebase-config-json.txt"